// We require the Hardhat Runtime Environment explicitly here. This is optional
// but useful for running the script in a standalone fashion through `node <script>`.
//
// When running the script with `npx hardhat run <script>` you'll find the Hardhat
// Runtime Environment's members available in the global scope.
// import { ethers } from "hardhat";

// async function main() {
//   // Hardhat always runs the compile task when running scripts with its command
//   // line interface.
//   //
//   // If this script is run directly using `node` you may want to call compile
//   // manually to make sure everything is compiled
//   // await hre.run('compile');

//   // We get the contract to deploy
//   const Greeter = await ethers.getContractFactory("Greeter");
//   const greeter = await Greeter.deploy("Hello, Hardhat!");

//   await greeter.deployed();

//   console.log("Greeter deployed to:", greeter.address);
// }

// // We recommend this pattern to be able to use async/await everywhere
// // and properly handle errors.
// main().catch((error) => {
//   console.error(error);
//   process.exitCode = 1;
// });

// Local network detected! Deploying factory... 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
// deploying "CrowdfundFactory" (tx: 0x1475f71988ba47a44f6e2d66fb72be4865a1b250c22e4e8a114d5aa846906a83)...:
// deployed at 0x5FbDB2315678afecb367f032d93F642f64180aa3 with 2373189 gas
// CrowdfundFactory Deployed!
// ----------------------------------
// You are deploying to a local network, you'll need a local network running to interact
// Please run `yarn hardhat console` to interact with the deployed smart contracts!
// ----------------------------------
